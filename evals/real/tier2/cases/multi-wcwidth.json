{
  "id": "multi-wcwidth",
  "category": "real",
  "goal": "Two independent regressions: (1) printable ASCII characters report width 2 instead of 1; (2) the interval-table binary search never reports a hit, so wide and zero-width lookups all fail. Find and fix both, then run the `verify` check.",
  "fixture": "multi-wcwidth",
  "approval_policy": "approve_all",
  "recipes": {
    "verify": {
      "argv": [
        "{python}",
        "-m",
        "pytest",
        "-q",
        "-x",
        "-p",
        "no:cacheprovider",
        "-o",
        "addopts=",
        "-k",
        "not package_version",
        "tests"
      ],
      "timeout_seconds": 180.0
    }
  },
  "expect": {
    "status": "succeeded",
    "allowed_changed_files": [
      "wcwidth/_wcwidth.py",
      "wcwidth/bisearch.py"
    ]
  }
}