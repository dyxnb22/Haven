{
  "id": "multi-jmespath",
  "category": "real",
  "goal": "Two independent regressions: (1) the `<` comparison operator treats equal values as less-than; (2) the `contains()` function returns the opposite of the correct answer. Find and fix both, then run the `verify` check.",
  "fixture": "multi-jmespath",
  "approval_policy": "approve_all",
  "recipes": {
    "verify": {
      "argv": [
        "{python}",
        "-m",
        "pytest",
        "-q",
        "-x",
        "-p",
        "no:cacheprovider",
        "tests"
      ],
      "timeout_seconds": 180.0
    }
  },
  "expect": {
    "status": "succeeded",
    "allowed_changed_files": [
      "jmespath/visitor.py",
      "jmespath/functions.py"
    ]
  }
}