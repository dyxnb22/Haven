{
  "id": "impl-jmespath-map",
  "category": "real",
  "goal": "The JMESPath built-in `map(&expr, array)` is missing from this build: expressions using it fail with UnknownFunctionError. Implement it \u2014 it applies the given expression reference to every element of the array and returns the list of results (nulls included). Nearby functions like sort_by show how expression references are evaluated. Then run the `verify` check.",
  "fixture": "impl-jmespath-map",
  "approval_policy": "approve_all",
  "recipes": {
    "verify": {
      "argv": [
        "{python}",
        "-m",
        "pytest",
        "-q",
        "-x",
        "-p",
        "no:cacheprovider",
        "tests"
      ],
      "timeout_seconds": 180.0
    }
  },
  "expect": {
    "status": "succeeded",
    "allowed_changed_files": [
      "jmespath/functions.py"
    ]
  }
}