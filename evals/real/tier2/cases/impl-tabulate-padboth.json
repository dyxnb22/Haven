{
  "id": "impl-tabulate-padboth",
  "category": "real",
  "goal": "Center alignment in tabulate raises NotImplementedError: the body of `_padboth` (pad a string on both sides to the given display width) was never written. Implement it consistently with `_padleft` and `_padright`, and run the `verify` check.",
  "fixture": "impl-tabulate-padboth",
  "approval_policy": "approve_all",
  "recipes": {
    "verify": {
      "argv": [
        "{python}",
        "-m",
        "pytest",
        "-q",
        "-x",
        "-p",
        "no:cacheprovider",
        "test"
      ],
      "timeout_seconds": 180.0
    }
  },
  "expect": {
    "status": "succeeded",
    "allowed_changed_files": [
      "tabulate/__init__.py"
    ]
  }
}