{
  "id": "impl-idna-label-length",
  "category": "real",
  "goal": "This idna build is missing the label length validation helper: encoding raises NameError: name 'valid_label_length' is not defined. Implement the missing function with the correct DNS length rule for labels (both str U-labels and bytes A-labels are passed to it) and run the `verify` check.",
  "fixture": "impl-idna-label-length",
  "approval_policy": "approve_all",
  "recipes": {
    "verify": {
      "argv": [
        "{python}",
        "-m",
        "pytest",
        "-q",
        "-x",
        "-p",
        "no:cacheprovider",
        "tests"
      ],
      "timeout_seconds": 180.0
    }
  },
  "expect": {
    "status": "succeeded",
    "allowed_changed_files": [
      "idna/core.py"
    ]
  }
}