{
  "id": "t4-rich-md-table-code",
  "category": "real",
  "goal": "A user reports: \"Inline code (backticks) inside a Markdown table cell loses its code styling when rendered \u2014 the same inline code outside a table renders with the code style. Something in the table-cell path flattens the styling.\" Find the cause, fix it, then run the `verify` check.",
  "fixture": "t4-rich-md-table-code",
  "approval_policy": "approve_all",
  "recipes": {
    "verify": {
      "argv": [
        "{python}",
        "-m",
        "pytest",
        "-q",
        "-x",
        "-p",
        "no:cacheprovider",
        "-p",
        "no:respx",
        "tests"
      ],
      "timeout_seconds": 180.0
    }
  },
  "expect": {
    "status": "succeeded",
    "allowed_changed_files": [
      "rich/markdown.py"
    ]
  },
  "budget": {
    "max_input_tokens": 800000
  }
}