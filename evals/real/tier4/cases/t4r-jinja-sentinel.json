{
  "id": "t4r-jinja-sentinel",
  "category": "real",
  "goal": "Refactor: move the `missing` sentinel (the `_MissingType` class and its singleton) out of jinja2's utils module into a new module `src/jinja2/_sentinel.py`. utils must re-export it (public imports keep working), and runtime.py and environment.py must import `missing` directly from the new module (`from ._sentinel import missing`). Behavior is unchanged; the full test suite must stay green. tests/test_task_refactor.py pins the required shape \u2014 do not modify it. A change like this spans several files; prefer a single repo.apply_patch. Then run the `verify` check.",
  "fixture": "t4r-jinja-sentinel",
  "approval_policy": "approve_all",
  "recipes": {
    "verify": {
      "argv": [
        "{python}",
        "-m",
        "pytest",
        "-q",
        "-x",
        "-p",
        "no:cacheprovider",
        "-p",
        "no:respx",
        "tests"
      ],
      "timeout_seconds": 180.0
    }
  },
  "expect": {
    "status": "succeeded",
    "allowed_changed_files": [
      "src/jinja2/_sentinel.py",
      "src/jinja2/utils.py",
      "src/jinja2/runtime.py",
      "src/jinja2/environment.py"
    ]
  },
  "budget": {
    "max_input_tokens": 800000
  }
}