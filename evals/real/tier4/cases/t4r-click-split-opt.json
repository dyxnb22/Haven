{
  "id": "t4r-click-split-opt",
  "category": "real",
  "goal": "Refactor: extract the option-prefix splitting helper `_split_opt` out of click's parser module into a new internal module `src/click/_parsing.py`. The parser, core, and formatting modules must import it from the new module (`from ._parsing import _split_opt`), and the function must no longer be defined in parser.py. Behavior is unchanged; the full test suite must stay green. tests/test_task_refactor.py pins the required shape \u2014 do not modify it. A change like this spans several files; prefer a single repo.apply_patch. Then run the `verify` check.",
  "fixture": "t4r-click-split-opt",
  "approval_policy": "approve_all",
  "recipes": {
    "verify": {
      "argv": [
        "{python}",
        "-m",
        "pytest",
        "-q",
        "-x",
        "-p",
        "no:cacheprovider",
        "-p",
        "no:respx",
        "-k",
        "not path_dash_no_byteswarning and not echo_via_pager",
        "tests"
      ],
      "timeout_seconds": 180.0
    }
  },
  "expect": {
    "status": "succeeded",
    "allowed_changed_files": [
      "src/click/_parsing.py",
      "src/click/parser.py",
      "src/click/core.py",
      "src/click/formatting.py"
    ]
  },
  "budget": {
    "max_input_tokens": 800000
  }
}